<h1>
	<?=$this->safe($country->getFullCountryName())?>
	[<?=$this->safe($country->getCoords(array('x', 'y', 'z')))?>]
</h1>

<h4>Your co-planet inhabitants</h4>

<table class="table table-hover table-striped">
	<thead>
		<tr>
			<th><?=_('Hello World')?></th>
			<th>Ruler and  Country</th>
			<th>Size</th>
			<th>Score</th>
		</tr>
	</thead>
	<tbody>
		<?php foreach ($planet->getIterator() as $country) { ?>
			<tr>
				<td><?=$country->getCoords(array('x', 'y', 'z'))?></td>
				<td><?=$country->getInfo('country_ruler_name')?> of <?=$country->getInfo('country_name')?></td>
				<td><?=$country->getInfo('country_asteroid_count')?></td>
				<td><?=Core_Format::number($country->getInfo('country_score'))?></td>
			</tr>
		<?php } ?>
	</tbody>
</table>